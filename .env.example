# Example environment variables for local/dev/prod
# Copy this file to .env locally (do NOT commit .env)

# --- Database ---
# LOCAL fallback:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/godrive?schema=public

# PRODUCTION (Neon) - set this in Render (and in your local .env when running Prisma)
DATABASE_URL=__SET_ME__

# Optional but recommended for Neon when using the pooler URL:
# Use the NON-pooler "Direct connection" string from Neon here.
DIRECT_URL=__OPTIONAL_SET_ME__

# --- Mercado Pago ---
MERCADO_PAGO_ACCESS_TOKEN=__SET_ME__

# --- URLs (used for Mercado Pago webhook/back URLs) ---
FRONTEND_URL=http://localhost:8081
BACKEND_URL=http://localhost:3000

NODE_ENV=development
